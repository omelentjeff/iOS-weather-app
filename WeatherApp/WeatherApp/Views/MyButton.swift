//
//  MyButton.swift
//  WeatherApp
//
//  Created by Otto Melentjeff on 5.5.2024.
//

import SwiftUI

/*struct MyButton: View {
    var image: String
    var color: Color
    var label: String
    var textColor: Color
    var action: () -> Void
    
    var body: some View {
        VStack {
            Button(action: action, label: {
                Image(systemName: image)
                    .padding(EdgeInsets(top: 20, leading: 20, bottom: 1, trailing: 20))
                    .font(.system(size: 24))
                    .foregroundStyle(color)
                //.cornerRadius(20)
            })
            Text(label).foregroundColor(textColor).font(.system(size: 12)).padding(EdgeInsets(top: 0, leading: 20, bottom: 0, trailing: 20))
        }
    }
}
*/
